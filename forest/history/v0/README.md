# Forest snapshot v0

This directory preserves the active forest immediately before design revision 1 was ported.
Files here are historical evidence and are excluded from the active registry and citation checks.
Use `design/history/v0/` for the byte-for-byte original Markdown sources.
