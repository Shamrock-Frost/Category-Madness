# CLAUDE.md

Read `AGENTS.md`: it is the operating manual for this repository (how to find a decision, what a PR must cite, what the seal forbids). Design decisions live in `forest/` and are cited by ID (`D-RT-03`) or address (`dec-rt-0003`).
